/**
 * Loads the outline from the given file.
 * @param {string} outlineInputFilename - the name of the file to load the outline from.
 * @param {object} options - the options to pass to the outline parser.
 * @returns {object} the outline object.
 */
function loadMarkdownOutline(outlineInputFilename, options) {

}

module.exports = {
    loadMarkdownOutline
}